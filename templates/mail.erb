    server {
        listen   <%= scope.lookupvar('listen_address') -%>:<%= scope.lookupvar('_listen_port') -%>;
        protocol <%= scope.lookupvar('protocol') -%>;
        proxy    <%= scope.lookupvar('proxy') -%>;
    }

