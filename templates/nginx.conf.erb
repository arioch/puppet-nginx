user              <%= @daemon_user %>;
worker_priority   <%= @worker_priority %>;
worker_processes  <%= @worker_processes %>;

error_log         <%= @logdir %>/error.log;
pid               <%= @pidfile %>;

