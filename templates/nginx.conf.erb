user              <%= scope.lookupvar('nginx::daemon_user') %>;
worker_processes  <%= scope.lookupvar('nginx::worker_processes') %>;

error_log         <%= scope.lookupvar('nginx::logdir') %>/error.log;
pid               <%= scope.lookupvar('nginx::pidfile') %>;

