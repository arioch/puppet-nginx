    use                <%= scope.lookupvar('nginx::event_model') %>;
    worker_connections <%= scope.lookupvar('nginx::worker_connections') %>;
    multi_accept       <%= scope.lookupvar('nginx::multi_accept') %>;
    accept_mutex       <%= scope.lookupvar('nginx::accept_mutex') %>;
