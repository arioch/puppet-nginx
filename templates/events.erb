    use                <%= @event_model %>;
    worker_connections <%= @worker_connections %>;
    multi_accept       <% if @multi_accept -%>on<% else -%>off<% end -%>;
    accept_mutex       <% if @accept_mutex -%>on<% else -%>off<% end -%>;
