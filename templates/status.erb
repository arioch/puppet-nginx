server {
    server_name localhost;
    location /nginx_status {
        stub_status on;
        access_log  off;
        allow       <%= scope.lookupvar('nginx::status_allow') %>;
        deny        <%= scope.lookupvar('nginx::status_deny') %>;
    }
}

