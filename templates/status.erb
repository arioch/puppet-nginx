server {
    server_name localhost;
    location /nginx_status {
        stub_status on;
        access_log  off;
        allow       <%= @status_allow %>;
        deny        <%= @status_deny %>;
    }
}

